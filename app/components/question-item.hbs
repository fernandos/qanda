<LinkTo 
  @route="question"
  @model={{@question.id}}
  class="list-group-item list-group-item-action d-flex gap-3 py-3">
  
  <div width="32" height="32" class="rounded-circle flex-shrink-0">👤💬</div>
  <div class="d-flex gap-2 w-100 justify-content-between">
    <div>
      <h6 class="mb-0">{{@question.title}}</h6>
      <p class="mb-0 opacity-75">{{@question.description}}</p>
      <p class="mb-0 opacity-75">Answers: {{@question.answersCount}}</p>
    </div>
    <small class="opacity-50 text-nowrap">{{@question.createdAt}}</small>
  </div>
</LinkTo>